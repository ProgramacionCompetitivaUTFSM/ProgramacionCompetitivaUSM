#include <bits/stdc++.h>
using namespace std;

typedef vector<long long int> vi;
typedef pair<int, int> ii;
typedef vector< ii > vii;
typedef vector< vi > graph;
typedef vector< vii > graphw;

int main(){
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL);
    cout.setf(ios::fixed);
    cout.precision(5);
    long long int i, n, S;
    double radio;
    cin >> radio;
    cout << (radio*(2 + sqrt(2) + sqrt(6)))*(radio*(2 + sqrt(2) + sqrt(6))) << "\n";
    return 0;
}